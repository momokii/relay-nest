{
  "name": "waha-command-center",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.12.4",
  "engines": {
    "node": ">=22.23.1 <23"
  },
  "scripts": {
    "build": "pnpm --filter @waha-command-center/config build && pnpm --filter @waha-command-center/domain build && pnpm --filter @waha-command-center/waha-contracts build && pnpm --filter @waha-command-center/api build && pnpm --filter @waha-command-center/web build",
    "db:generate": "pnpm --filter @waha-command-center/api db:generate",
    "db:migrate": "pnpm --filter @waha-command-center/api db:migrate",
    "dev": "VITE_API_BASE_URL= VITE_API_PROXY_TARGET=http://127.0.0.1:3000 pnpm --parallel --filter @waha-command-center/api --filter @waha-command-center/web dev",
    "dev:bundled": "docker compose -p relaynest-dev -f docker-compose.yml -f docker-compose.override.yml -f docker-compose.bundled-waha.yml --profile waha up --build --wait -d",
    "deploy:bundled": "docker compose -p relaynest -f docker-compose.yml -f docker-compose.override.yml -f docker-compose.bundled-waha.yml --profile waha up --build --wait -d",
    "deploy:external": "docker compose -p relaynest -f docker-compose.yml -f docker-compose.override.yml -f docker-compose.external-waha.yml up --build --wait -d",
    "deploy:down": "docker compose -p relaynest -f docker-compose.yml -f docker-compose.override.yml --profile waha down --remove-orphans",
    "healthcheck": "bash scripts/healthcheck.sh",
    "setup:bundled": "bash scripts/setup.sh bundled",
    "setup:external": "bash scripts/setup.sh external",
    "setup:dev": "bash scripts/setup.sh dev",
    "shutdown": "bash scripts/shutdown.sh",
    "trust": "bash scripts/trust-report.sh",
    "e2e": "playwright test",
    "feature": "node --experimental-strip-types scripts/feature-check.mts",
    "lint": "biome check .",
    "release": "bash scripts/release.sh",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "typecheck": "tsc -b --pretty false",
    "verify:requirements": "node --experimental-strip-types scripts/release-checks.mts requirements",
    "secret-scan": "node --experimental-strip-types scripts/release-checks.mts secrets",
    "verify:scope": "node --experimental-strip-types scripts/release-checks.mts scope",
    "docs:check": "node --experimental-strip-types scripts/release-checks.mts docs",
    "doctor": "npx react-doctor@latest"
  },
  "pnpm": {
    "overrides": {
      "esbuild": "0.25.12"
    }
  },
  "devDependencies": {
    "@biomejs/biome": "2.0.6",
    "@playwright/test": "1.55.1",
    "@types/node": "22.15.21",
    "typescript": "5.8.3",
    "vitest": "3.2.6"
  }
}
